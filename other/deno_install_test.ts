console.log('deno install test');


/* 
USAGE:

-- install: grab the raw url of this gist in the `raw` button (maybe minus the commitid part) and do:
deno install https://gist.githubusercontent.com/drodsou/bf9f8ba2f3ec99f06c3499b0de696f65/raw/deno_install_test.ts

-- that auto generates a proper $HOME/.deno/bin bash or cmd script that you can run with:_
deno_install_test

-- --allow permissions are specified in the deno install command, so you dont need to add as parameter to the script call every time

-- more info: https://deno.land/manual/tools/script_installer
*/
